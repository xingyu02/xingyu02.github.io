<!DOCTYPE html>
<html lang="zh-Hant">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>陳幸妤｜菜系主題封面</title>
  <link href="https://fonts.googleapis.com/css2?family=Noto+Serif+TC&display=swap" rel="stylesheet">
  <style>
    body {
      margin: 0;
      font-family: "Noto Serif TC", serif;
      background-color: #f9f9f6;
      color: #1c2b38;
    }

    header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 20px 40px;
      border-bottom: 1px solid #ddd;
    }

    nav a {
      margin: 0 15px;
      text-decoration: none;
      color: #1c2b38;
      font-weight: 500;
    }

    .hero {
      display: flex;
      flex-direction: column;
      align-items: center;
      padding: 80px 20px;
    }

    .hero img {
      width: 300px;
      margin-bottom: 40px;
    }

    .vertical-text {
      writing-mode: vertical-rl;
      text-orientation: upright;
      font-size: 20px;
      margin-bottom: 10px;
      line-height: 1.8;
    }

    .title {
      font-size: 48px;
      letter-spacing: 10px;
      margin-top: 30px;
    }

    .play-button {
      width: 60px;
      height: 60px;
      border-radius: 50%;
      background-color: rgba(255,255,255,0.6);
      border: 2px solid #1c2b38;
      display: flex;
      align-items: center;
      justify-content: center;
      position: absolute;
      bottom: 20px;
      right: 20px;
    }

    .play-button::before {
      content: '';
      display: block;
      width: 0;
      height: 0;
      border-left: 15px solid #1c2b38;
      border-top: 10px solid transparent;
      border-bottom: 10px solid transparent;
      margin-left: 4px;
    }

    @media (min-width: 768px) {
      .hero {
        flex-direction: row;
        justify-content: center;
      }

      .hero img {
        margin-right: 50px;
        margin-bottom: 0;
      }

      .info {
        display: flex;
        flex-direction: column;
        justify-content: center;
      }
    }
  </style>
</head>
<body>

  <header>
    <div style="font-weight:bold;">深川製碗</div>
    <nav>
      <a href="#">HOME</a>
      <a href="#">ABOUT</a>
      <a href="#">PRODUCTS</a>
      <a href="#">SHOP</a>
    </nav>
  </header>

  <section class="hero">
    <div style="position: relative;">
      <img src="vegetable-bowl.png" alt="菜系主題圖像" />
      <div class="play-button"></div>
    </div>

    <div class="info">
      <div class="vertical-text">陳幸妤</div>
      <div class="vertical-text">華文二甲</div>
      <div class="title">菜系</div>
    </div>
  </section>

</body>
</html>
